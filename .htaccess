# mod_rewrite starts here

RewriteEngine on

#does not apply ti existing directories
RewriteCond %{REQUEST_FILENAME}!-d

#check for files in directories with html extension
RewriteCond %{REQUEST_FILENAME}\.html -f

RewriteRule ^(.*)$ $1.html[NC,L]
RewriteRule ^([^\.]+)$ $1.html [NC,L]